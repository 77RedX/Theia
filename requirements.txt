# Theia Video Enhancer — Python dependencies
# Install with:  pip install -r requirements.txt

# GUI framework
PyQt6>=6.6.0

# ── Future: add when Person 3 integrates the engine ──────────────────────
# onnxruntime>=1.17.0       # CPU inference
# onnxruntime-gpu>=1.17.0   # GPU inference (choose one)
# torch>=2.2.0              # PyTorch fallback inference
# ffmpeg-python>=0.2.0      # FFmpeg Python bindings (Person 3)
# opencv-python>=4.9.0      # Frame extraction (Person 3)
# numpy>=1.26.0             # Array ops (Person 3)
